import { ColorItem, ColorPalette, Meta } from '@storybook/blocks'
import { useEffect, useState } from 'react'
import { getColors } from './getColors'
import '../tokens/colors.css'

export const AllColors = () => {
  const [colorVariables, setColorVariables] = useState({})
  useEffect(() => {
    const fetchedColors = getColors()
    setColorVariables(fetchedColors)
  }, [])
  return (
    <ColorPalette>
      {Object.entries(colorVariables).map(([colorType, shades]) => (
        <ColorItem key={colorType} title={`theme.colors.${colorType}`} colors={shades} />
      ))}
    </ColorPalette>
  )
}

<Meta title="Tokens/Colors" />

# Colors

<AllColors />

# Gradients

<ColorPalette>
  <ColorItem
    title="Green to Light"
    colors={{
      '.btg-gradient-green':
        'linear-gradient(147deg, var(--btg-color-brand-green), var(--btg-color-brand-light))',
    }}
  />
  <ColorItem
    title="Pink to Light"
    colors={{
      '.btg-gradient-pink':
        'linear-gradient(147deg, var(--btg-color-brand-pink), var(--btg-color-brand-light))',
    }}
  />
  <ColorItem
    title="Yellow to Light"
    colors={{
      '.btg-gradient-yellow':
        'linear-gradient(147deg, var(--btg-color-brand-yellow), var(--btg-color-brand-light))',
    }}
  />
</ColorPalette>
